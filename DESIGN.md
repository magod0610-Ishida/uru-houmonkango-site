# Ulu 訪問看護ステーション｜デザインルール（案）

> 目的：Intercomの「情報整理の分かりやすさ」と Clay の「柔らかい図形・背景演出」を参考にしつつ、
> 訪問看護の公式サイトとして **安心感・清潔感・親しみ** を最優先にする。
> 派手さ・デザイン会社っぽさは避ける。

---

## 1. カラー

### メインカラー（現行のUluグリーンを維持）
既存の `tailwind.config.mjs` の `brand` スケールをそのまま使用する。

| 用途 | カラー | 備考 |
|---|---|---|
| メインアクション（ボタン等） | `brand-600` `#16a34a` | 通常状態 |
| ホバー | `brand-700` `#15803d` | ボタン・リンクhover |
| 見出しアクセント／アイコン | `brand-600` 〜 `brand-700` | |
| 淡い塗り（バッジ・タグ背景） | `brand-100` `#dcfce7` | |

### 背景色（白・薄クリーム・薄グリーンの3種を基本ローテーション）
| 用途 | カラー |
|---|---|
| ベース背景 | 白 `#FFFFFF` |
| やわらかい背景（既存） | クリーム `#FAF7F0` |
| 新規追加：薄いグリーン背景 | `brand-50` `#F0FDF4` |

→ 詳細は「6. セクション背景」参照。

### 差し色（アクセント）：1色のみ・使用量は全体の10%以内
- 提案色：**ソフトアプリコット** `#F2A65A`（暖色・柔らかい・訪問看護の親しみやすさに合う）
- 使い所の例（すべて小面積）：
  - 「採用情報」「新着」などの小さいバッジ
  - アイコンのワンポイント（電話・チェックマークなど）
  - お客様の声セクションの★や引用符
  - グラフ・ステップ番号などの1点強調
- **使わない場所**：メインボタン、ヘッダー、大面積の背景、複数箇所の同時使用

### 禁止事項
- グリーン・クリーム・アプリコット以外の新しい色相を増やさない
- 彩度の高い蛍光色、ネイビー、ビビッドな青・赤などは使わない

---

## 2. タイポグラフィ

日本語は文字が詰まって見えやすいため、Intercom的な「大きめ行間・太字は要所だけ」を採用。

| 要素 | サイズ（モバイル） | サイズ（PC） | 太さ | 行間 |
|---|---|---|---|---|
| H1（ページタイトル） | 28–32px | 40–44px | 700 (bold) | 1.4 |
| H2（セクション見出し） | 22–24px | 32px | 700 (bold) | 1.5 |
| H3（カード見出し等） | 18–20px | 20–22px | 600 (semibold) | 1.5 |
| 本文 | 15–16px | 16px | 400 (regular) | 1.8〜1.9 |
| 補足・キャプション | 13–14px | 13–14px | 400 | 1.6 |
| 強調テキスト | 本文と同サイズ | 同左 | 600（色は変えない、太さのみ） | — |

**ルール**
- 1画面内で太字（700）を使うのは見出しとCTAボタンのみ。本文中の強調は semibold(600) + 色は brand-700 程度に留める。
- 装飾的なフォント（手書き風・丸文字風など）は使わない。清潔感を優先し、既存のシステムフォントスタックを維持する。

---

## 3. 余白ルール（8 / 16 / 24 / 32 / 48 / 80 の6段階）

| トークン | 値 | 主な用途 |
|---|---|---|
| space-1 | 8px | アイコンとテキストの間、タグ内側の余白 |
| space-2 | 16px | カード内側の細かい要素間、フォーム項目間 |
| space-3 | 24px | カード内側の標準パディング、カード同士のgap |
| space-4 | 32px | セクション内の見出しと本文の間、ブロック間 |
| space-5 | 48px | サブセクション間の余白 |
| space-6 | 80px | セクションとセクションの間（大区切り） |

**モバイルでの縮小ルール**
- 80px → 48px、48px → 32px に一段階縮小（詰まりすぎない程度に圧縮）
- 8/16/24 はモバイルでもそのまま維持（縮小しない）

---

## 4. カードの角丸・影・枠線

| 要素 | 値 |
|---|---|
| 写真の角丸 | 8px |
| カードの角丸 | 16px |
| ボタンの角丸 | 24px（pill状に近い、完全な円形ではない） |
| 影（弱） | 既存の `shadow-softer`（`0 2px 14px -4px rgba(120,113,90,0.10)`）を標準に |
| 影（やや強め・ホバー時のみ） | 既存の `shadow-soft`（`0 4px 24px -6px rgba(120,113,90,0.15)`） |
| 枠線 | 基本なし。背景が白同士で隣接し境界が必要な場合のみ `1px solid brand-100` |

**ルール**
- 影は常に「弱→ホバーで少し強くなる」の一段階変化のみ。濃い影・複数影の重ねがけはしない。
- カード内で角丸16pxを崩さない（写真をカード内に入れる場合は写真だけ8px、カード全体は16pxを維持）。

---

## 5. ボタンのデザイン

### プライマリボタン
- 背景：`brand-600` / 文字：白 / 角丸：24px
- ホバー：背景 `brand-700`、わずかに上に浮く（`translateY(-1px)` 程度）+ 影が softer→soft に変化
- パディング：縦12–14px、横24–32px
- 文字太さ：600、サイズは本文と同じ

### セカンダリボタン（例：「詳しく見る」等）
- 背景：透明 or 白 / 枠線：`1px solid brand-600` / 文字：`brand-700`
- ホバー：背景を `brand-50` に薄く塗る程度

**ルール**
- ボタンの色数は2種（プライマリ／セカンダリ）まで。差し色（アプリコット）をボタン背景には使わない。
- モバイルでは主要CTAボタンは横幅いっぱい（`w-full`）にしてタップしやすくする。
- アイコン付きボタンは「テキスト＋矢印」程度に留め、装飾アイコンを乱用しない。

---

## 6. セクションごとの背景の使い分け

Clayのような「背景が緩やかに変化していく」印象を、色そのものは3色（白・クリーム・薄グリーン）に絞って表現する。

**基本パターン（上から下へのローテーション例）**
1. ヒーロー：白 または 薄グリーン（現状維持）＋背景にごく薄い葉モチーフ（既存の透かしを継続、不透明度5–8%程度）
2. サービス紹介：白
3. ご利用の流れ／ステップ：クリーム
4. お客様の声：薄グリーン
5. スタッフ紹介：白
6. 採用情報：クリーム
7. お問い合わせCTA（フッター手前）：薄グリーン、または brand-600 の淡いグラデーション程度（ベタ塗りの濃色は避ける）

**ルール**
- 同じ背景色を3セクション以上連続させない（単調になるため）。
- セクションの境界に線（border）を引かない。色の切り替えだけで区切りを作る。
- Clay的な柔らかい図形（ぼかした円・葉っぱモチーフなど）を背景装飾として使う場合、不透明度は常に10%以下、テキストの可読性を絶対に妨げないこと。

---

## 7. 写真の見せ方

- 角丸：8px で統一（カード16pxの内側に収める場合も写真自体は8px）
- 実際のスタッフ・利用者（許諾済み）の写真を優先し、ストック感の強い写真は避ける
- 色調：彩度を上げすぎない自然なトーン。過度なフィルターやビネットは使わない
- 影：写真自体には強い影をつけない（カードの影で十分）。写真単体で使う場合のみ `shadow-softer` を薄く
- テキストを写真に重ねる場合：白文字が必要な時は下から上への薄いグラデーション（黒 0%→40%程度）のみ使用し、写真全体を暗くしない
- 写真の比率はセクションごとに統一（例：スタッフ紹介は正方形 or 4:5、風景・施設写真は16:9 or 4:3）

---

## 8. PC／スマホのレスポンシブ方針

- ブレークポイントは Tailwind 標準（`sm:640 / md:768 / lg:1024`）に準拠
- モバイル（〜767px）
  - カード・写真は縦積み1カラム
  - ナビゲーションはハンバーガーメニューに格納
  - 主要CTAボタンは横幅いっぱい
  - 余白は「3. 余白ルール」の縮小ルールを適用
- タブレット（768〜1023px）
  - カードは2カラムまで
- PC（1024px〜）
  - カードは2〜3カラム、最大コンテンツ幅は現状維持（新たに広げすぎない）
- 見出し・本文とも「2. タイポグラフィ」のモバイル／PC値をそのまま使う（中間サイズを増やさない＝管理をシンプルに保つ）

---

## 9. やってよいデザイン／やらないデザイン

### やってよい ✅
- 背景色の緩やかな切り替え（白→クリーム→薄グリーン）でセクションにリズムを作る
- カード・ボタンの角に一貫した丸みを持たせ、やわらかい印象にする
- ごく薄い（10%以下）葉モチーフや円形のぼかし図形を背景装飾として使う
- ホバー時のわずかな浮き上がり・影の変化などマイクロインタラクションで柔らかさを出す
- 情報をカード単位で整理し、1カード＝1メッセージに絞る（Intercom的な整理）
- 実写真を主役にし、余白でゆったり見せる

### やらない ❌
- 差し色（アプリコット）を2箇所以上・大面積で同時使用しない
- 影を複数重ねる／濃い影を使う
- セクション間に太いborderや強いコントラストの区切り線を引く
- 装飾的すぎるフォント、極端に細い/太いウェイトの多用
- 背景装飾（Clay的な図形）を可読性より優先させる、または動きを激しくする（パララックスの多用・派手なアニメーション）
- 派手なグラデーションボタンや、デザイン会社的な「尖った」ビジュアル（鋭角図形、ネオン、グリッチ表現等）
- 写真に強いフィルター・過度な加工をかけて不自然にする
- 訪問看護という業種の信頼感を損なう、遊び要素の強すぎる表現

---

## 参考にした考え方（要約）
- **Intercom的要素**：情報のグルーピングの明快さ、見出し階層の一貫性、カードごとに役割が一目でわかる整理、装飾を抑えた読みやすいタイポグラフィ
- **Clay的要素**：背景色・柔らかい図形によるセクション間の緩やかな変化、マイクロインタラクション、写真を活かす余白の取り方
- **Uluとして守ること**：グリーンのブランドカラー、清潔感、訪問看護としての信頼感、派手にしない
